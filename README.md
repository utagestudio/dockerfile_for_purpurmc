# Require
- Docker
- Docker compose

# Setup
```$ docker-compose build```

# Run
```$ docker-compose up -d```

# Stop
```$ docker-compose stop```

# Remove Container / network
```$ docker-compose down```

# Shell
```$ docker-compose run -it purpur /bin/bash```